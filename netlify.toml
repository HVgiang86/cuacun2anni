[build]
  command = "npm run build"
  publish = "out"

# SPA routing configuration (fallback to index.html for unknown routes if needed, 
# though Next.js static export usually handles this with the generated HTML files)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
